<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itchunyang.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CustomViewActivity1" />
        <activity android:name=".activity.AuthViewActivity" />
        <activity android:name=".activity.NumberProgressActivity" />
        <activity android:name=".activity.IndexBarActivity" />
        <activity android:name=".activity.MyViewGroupActivity" />
        <activity android:name=".activity.NavigationViewActivity" />
        <activity android:name=".activity.FlowLayoutActivity"></activity>
    </application>

</manifest>